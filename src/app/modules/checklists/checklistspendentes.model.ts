export interface ChecklistPendentes {
  solucionador: string;

  pendentes90: number;
  pendentes60a90: number;
  pendentes30a60: number;
  pendentes30: number;

  pendentes: number;
  concluidos: number;
}
